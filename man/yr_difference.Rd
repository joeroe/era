% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yr_difference.R
\name{yr_difference}
\alias{yr_difference}
\title{Chronological difference between year vectors}
\usage{
yr_difference(x, y)
}
\arguments{
\item{x}{A \link{yr} vector with era.}

\item{y}{A \link{yr} vector or numeric value. If \code{yr}, and in a different era than
\code{x}, it is transformed to the era of \code{x}. Recycled to the common length of
\code{x} and \code{y}.}
}
\value{
Numeric vector of signed differences. Positive values indicate \code{x} is
chronologically later than \code{y}; negative values indicate \code{x} is earlier.
}
\description{
\code{yr_difference(x, y)} calculates how much later \code{x} is than \code{y}, taking into
}
\details{
This function performs chronological subtraction, not numeric subtraction.
Both inputs are converted to a common chronological scale (accounting for era
direction and epoch), and the difference is computed as \code{x - y} on that
scale.

For forward-counting eras (e.g. CE), this is equivalent to numeric
subtraction. For backward-counting eras (e.g. BCE, BP), the era direction is
accounted for, so \code{yr_difference(yr(1900, "BCE"), yr(2000, "BCE"))} returns
100 (1900 BCE is 100 years later than 2000 BCE), even though 1900 - 2000 =
-100.
}
\examples{
# Forward-counting era (CE):
yr_difference(yr(200, "CE"), yr(100, "CE"))  # 100
yr_difference(yr(100, "CE"), yr(200, "CE"))  # -100

# Backward-counting era (BCE):
yr_difference(yr(1900, "BCE"), yr(2000, "BCE"))  # 100
yr_difference(yr(2000, "BCE"), yr(1900, "BCE"))  # -100

# Cross-era (accounts for year zero):
yr_difference(yr(10, "CE"), yr(10, "BCE"))  # 19

# Scaled eras (returns difference in era's native units):
yr_difference(yr(2, "ka"), yr(1, "ka"))  # 1
}
\seealso{
Other functions for chronological ordering and extremes:
\code{\link[=yr_earlier_than]{yr_earlier_than()}},
\code{\link{yr_extremes}},
\code{\link[=yr_sort]{yr_sort()}}
}
\concept{functions for chronological ordering and extremes}
